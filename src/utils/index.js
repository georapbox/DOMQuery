/**
 * @module utils/index
 */
module.exports = (function () {
    'use strict';

    return {
        is: require('utils/misc/is'),
        trim: require('utils/strings/trim')
    };
}());
